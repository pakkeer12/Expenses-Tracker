# Environment Variables Example
# Copy this file to .env and fill in your values

# Node Environment
NODE_ENV=development

# Database URL (leave empty for local SQLite, set for PostgreSQL in production)
# Local: leave empty (will use local.db)
# Production: postgresql://user:password@host:port/database
DATABASE_URL=

# Session Secret (use a random string in production)
SESSION_SECRET=your-secret-key-change-this-in-production

# Server Port
PORT=5000

# CORS Origins (comma-separated list for production)
# Example: https://your-app.com,https://www.your-app.com
CORS_ORIGINS=http://localhost:5000
